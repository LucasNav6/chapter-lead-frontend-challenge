import React from "react";
import {Icon} from "@iconify/react";

const Loader = () => {
  return (
    <main className="loader-page">
      <Icon width={75} icon="line-md:loading-loop" />
    </main>
  );
};

export default Loader;
